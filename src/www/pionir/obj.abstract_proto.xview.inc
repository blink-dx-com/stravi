<?php
function obj_selection() {
        $argu=array();
        $argu[] = array("url"=>"p.php?mod=DEF/o.ABSTRACT_PROTO.li_stepcomp", "txt"=>"Compare steps");
        $menu = array('func'=>$argu );
		return ($menu);
}
