<?

function obj_selection() {

    $argu=array();	
    $argu[] = array("url"=>"obj.contact.track_list.php", "txt"=>"[ProtoQuant] Samples > Protocols");
    
    $menu = array('func'=>$argu );
	
	return ($menu);
}